<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.n52.sensorweb.sos</groupId>
        <artifactId>dynamic-modeling</artifactId>
        <version>4.4.0-SNAPSHOT</version>
    </parent>

    <artifactId>hydrology-modeling-sos-plugin</artifactId>
    <packaging>jar</packaging>
    <name>52°North SOS - Hydrology Modeling Extension</name>
    <description>52°North SOS extension to integrate Hydrologic models (EPANET/SWMM/HEC-RAS) as WaterML Sensors/Observation objects</description>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.n52.sensorweb.sos</groupId>
            <artifactId>dynamic-modeling-sos-converter</artifactId>
            <version>${version}</version>
        </dependency>

        <!-- Hydraulic modeling core dependencies -->
        <dependency>
            <groupId>org.n52.sensorweb.sos</groupId>
            <artifactId>hydrology-core-library</artifactId>
            <version>1.0</version>
        </dependency>

        <!-- Spring framework dependencies -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-beans</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>

        <!-- Test framework dependencies -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>
    <repositories>
        <repository>
            <id>osgeo</id>
            <name>Open Source Geospatial Foundation Repository</name>
            <url>http://download.osgeo.org/webdav/geotools/</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>    
</project>
